LT_REGISTER({
  id: "golang",
  title: "Golang",
  color: "#00ADD8",
  description: "Go с нуля: синтаксис, типы, функции, горутины. Каждая конструкция — с объяснением, что она принимает и что возвращает.",
  modules: [
    {
      id: "basics",
      title: "Модуль 1. Основы языка",
      lessons: [
        {
          id: "go-01",
          title: "Установка, первая программа, fmt.Println",
          theory: `
## Установка

1. Скачай Go с https://go.dev/dl/ (для Windows — .msi, ставится в пару кликов).
2. Проверь в терминале:

\`\`\`
go version
// должно вывести что-то вроде: go version go1.24.x windows/amd64
\`\`\`

## Первая программа

Создай папку проекта и файл \`main.go\`:

\`\`\`go
package main // каждый файл Go начинается с объявления пакета.
// Пакет "main" — особенный: именно из него собирается исполняемая программа.

import "fmt" // подключаем пакет fmt (форматированный ввод/вывод).
// Без import использовать fmt.Println нельзя — Go не даст скомпилировать
// программу и с НЕиспользуемым импортом тоже (это ошибка, а не предупреждение).

func main() { // функция main() — точка входа. Не принимает ничего, не возвращает ничего.
	// Программа начинается с первой строки main и заканчивается на последней.
	fmt.Println("Привет, Go!")
}
\`\`\`

Запуск:

\`\`\`
go run main.go   // компилирует во временный файл и сразу запускает
go build         // просто собирает .exe в текущей папке
\`\`\`

## fmt.Println подробно

\`\`\`go
fmt.Println("строка", "их может быть несколько", 234, true)
// ЧТО ПРИНИМАЕТ: любое количество аргументов ЛЮБОГО типа (сигнатура: a ...any).
// ЧТО ДЕЛАЕТ: печатает все аргументы через пробел и добавляет перевод строки \\n в конце.
// ЧТО ВЫВЕДЕТ: строка их может быть несколько 234 true
// ЧТО ВОЗВРАЩАЕТ: (n int, err error) — сколько байт напечатано и ошибку.
// Возвращаемые значения почти всегда игнорируют, поэтому пишут просто fmt.Println(...).
\`\`\`

Соседи по пакету:

\`\`\`go
fmt.Print("без", "перевода строки") // то же самое, но БЕЗ \\n в конце
// и пробел ставит только между аргументами, ни один из которых не строка.
// Выведет: безперевода строки  (между "без" и "перевода" пробела НЕТ — обе строки)

fmt.Printf("число: %d, строка: %s\\n", 42, "привет")
// ЧТО ПРИНИМАЕТ: первый аргумент — строка-шаблон, дальше — значения для подстановки.
// %d — целое число, %s — строка, %f — дробное, %t — bool, %v — "что угодно".
// \\n нужно ставить самому — Printf его не добавляет.
// ЧТО ВЫВЕДЕТ: число: 42, строка: привет
\`\`\`
`,
          homework: `
1. Установи Go, добейся, чтобы \`go version\` работал.
2. Напиши программу, которая выводит три строки: твоё имя, возраст (числом, через %d) и цель изучения Go.
3. Попробуй оставить неиспользуемый import и пришли мне текст ошибки компилятора + объясни своими словами, что Go этим хочет сказать.
`,
          cards: [
            { q: "С чего начинается каждый файл Go?", a: "С объявления пакета: package имя. Исполняемая программа собирается из пакета main" },
            { q: "Что принимает fmt.Println?", a: "Любое количество аргументов любого типа (...any); печатает через пробел и добавляет \\n" },
            { q: "Что возвращает fmt.Println?", a: "(n int, err error) — сколько байт напечатано и ошибку; обычно игнорируют" },
            { q: "Чем fmt.Print отличается от fmt.Println?", a: "Не добавляет \\n в конце и ставит пробел только между нестроковыми аргументами" },
            { q: "Что будет, если в Go оставить неиспользуемый import?", a: "Ошибка компиляции (не предупреждение!) — программа не соберётся" },
            { q: "Чем go run отличается от go build?", a: "go run компилирует во временный файл и сразу запускает; go build создаёт исполняемый файл в папке" }
          ]
        },
        {
          id: "go-02",
          title: "Переменные, типы, константы",
          theory: `
## Объявление переменных

\`\`\`go
var age int = 17
// var ИмяПеременной тип = значение
// "int" здесь — не обязательное слово, а МЕСТО, куда пишется нужный тип:
// int, string, float64, bool и т.д.

var name = "Вова" // тип можно не писать — Go сам выведет его из значения (тут string)

city := "Москва" // короткая форма: объявить + присвоить. Работает ТОЛЬКО внутри функций.
// := создаёт НОВУЮ переменную; = присваивает в уже существующую. Их путают чаще всего.

var x, y int = 10, 20 // можно объявить несколько сразу
\`\`\`

## Базовые типы

| Тип | Что хранит | Пример |
|---|---|---|
| \`int\` | целые числа | \`42\`, \`-7\` |
| \`float64\` | дробные | \`3.14\` |
| \`string\` | строки (неизменяемые!) | \`"привет"\` |
| \`bool\` | логика | \`true\` / \`false\` |
| \`byte\` | один байт (синоним uint8) | \`'A'\` |
| \`rune\` | один символ Unicode (синоним int32) | \`'Я'\` |

> Важно: Go **строго типизирован**. Сложить \`int\` и \`float64\` без явного преобразования нельзя:

\`\`\`go
a := 5        // int
b := 2.5      // float64
// c := a + b // ОШИБКА КОМПИЛЯЦИИ: mismatched types int and float64
c := float64(a) + b
// float64(a) — преобразование типа.
// ЧТО ПРИНИМАЕТ: значение другого числового типа. ЧТО ВОЗВРАЩАЕТ: то же число как float64.
fmt.Println(c) // выведет: 7.5
\`\`\`

## Нулевые значения

Переменная без явного значения не «мусор», как в C, а нулевое значение типа:

\`\`\`go
var n int    // 0
var s string // "" (пустая строка)
var b bool   // false
var f float64 // 0
\`\`\`

## Константы

\`\`\`go
const Pi = 3.14159 // значение задаётся один раз, менять нельзя
// const работает и вне функций; := — нет.
\`\`\`
`,
          homework: `
1. Заведи переменные всеми тремя способами (\`var\` с типом, \`var\` без типа, \`:=\`) и выведи их одним \`fmt.Println\`.
2. Напиши программу: температура в цельсиях (float64) → в фаренгейты (F = C×9/5 + 32). Выведи через \`fmt.Printf\` с двумя знаками после запятой (\`%.2f\`).
3. Объясни мне своими словами разницу между \`:=\` и \`=\` — пришлю встречный каверзный вопрос.
`,
          cards: [
            { q: "Разница между := и = в Go?", a: ":= объявляет НОВУЮ переменную (только внутри функций), = присваивает в существующую" },
            { q: "Чему равна var s string без присваивания?", a: "\"\" — пустой строке. В Go всё инициализируется нулевым значением типа, мусора не бывает" },
            { q: "Можно ли в Go сложить int и float64 напрямую?", a: "Нет, ошибка компиляции — нужно явное преобразование: float64(a) + b" },
            { q: "Что такое rune в Go?", a: "Один символ Unicode (синоним int32). byte — один байт (uint8)" },
            { q: "Нулевые значения int, string, bool?", a: "0, \"\" (пустая строка), false" }
          ]
        },
        {
          id: "go-03",
          title: "Условия и циклы: if, switch, for, range",
          theory: `
## if / else

\`\`\`go
if age >= 18 { // скобки вокруг условия НЕ нужны, а фигурные скобки ОБЯЗАТЕЛЬНЫ всегда
	fmt.Println("совершеннолетний")
} else if age >= 14 {
	fmt.Println("паспорт уже есть")
} else {
	fmt.Println("школьник")
} // else пишется на ОДНОЙ строке с закрывающей скобкой — иначе ошибка компиляции

if n := 10; n%2 == 0 { // можно объявить переменную прямо в if;
	// n живёт только внутри этого if/else
	fmt.Println("чётное")
}
\`\`\`

## switch

\`\`\`go
switch day { // сравнивает day с каждым case по очереди
case 6, 7: // можно несколько значений через запятую
	fmt.Println("выходной")
case 1:
	fmt.Println("понедельник...")
default: // если ничего не подошло
	fmt.Println("будни")
}
// В отличие от C/Java, break НЕ нужен — Go сам выходит после сработавшего case.
\`\`\`

## for — единственный цикл в Go

Циклов while и do-while нет, всё делает for:

\`\`\`go
for i := 0; i < 5; i++ { // классика: старт; условие продолжения; шаг
	fmt.Println(i) // выведет 0 1 2 3 4 (каждое с новой строки)
}

for x < 100 { // for с одним условием — это и есть while
	x *= 2
}

for { // вообще без условия — бесконечный цикл
	break // break — выйти из цикла; continue — перейти к следующей итерации
}
\`\`\`

## range — перебор коллекций

\`\`\`go
nums := []int{10, 20, 30} // срез (динамический массив) — подробно в следующем уроке

for ind, elem := range nums {
	// ind и elem могут называться КАК УГОДНО (i, v, index, value...).
	// Важен порядок: ПЕРВАЯ переменная — индекс, ВТОРАЯ — КОПИЯ элемента.
	// Менять elem бесполезно — исходный срез не изменится (это копия!).
	fmt.Println(ind, elem)
}
// выведет:
// 0 10
// 1 20
// 2 30

for _, elem := range nums { // индекс не нужен? подчёркивание _ = "игнорирую это значение"
	fmt.Println(elem)
}

for ind := range nums { // а вторую переменную можно просто не писать — останутся индексы
	fmt.Println(ind)
}
\`\`\`

> Для строк \`range\` перебирает не байты, а руны (символы Unicode) — русские буквы обрабатываются корректно, но индекс прыгает через 2 (кириллица занимает 2 байта).
`,
          homework: `
1. FizzBuzz: числа от 1 до 30; кратно 3 → "Fizz", кратно 5 → "Buzz", кратно 15 → "FizzBuzz", иначе само число.
2. Через \`for ... range\` посчитай сумму и максимум среза \`[]int{7, 2, 19, 4, 15}\` за один проход.
3. Пришли мне: что выведет \`for i, c := range "Го!"\` и почему индексы такие странные.
`,
          cards: [
            { q: "Какие циклы есть в Go?", a: "Только for. Он же работает как while (for x < 10) и как бесконечный цикл (for {})" },
            { q: "Что дают переменные в for i, v := range срез?", a: "Первая — индекс, вторая — КОПИЯ элемента (менять её бесполезно). Имена произвольные" },
            { q: "Нужен ли break в switch в Go?", a: "Нет — Go сам выходит после сработавшего case (в отличие от C/Java)" },
            { q: "Как проигнорировать индекс в range?", a: "Подчёркиванием: for _, v := range ... (_ = «игнорирую это значение»)" },
            { q: "Что перебирает range по строке?", a: "Руны (символы Unicode), а не байты — поэтому индексы кириллицы прыгают через 2" }
          ]
        }
      ]
    },
    {
      id: "collections",
      title: "Модуль 2. Коллекции и функции",
      lessons: [
        {
          id: "go-04",
          title: "Массивы, срезы, map",
          theory: `
## Массив — фиксированная длина

\`\`\`go
var arr [5]int
// var ИмяМассива [N]тип
// [5] — длина, часть типа! [5]int и [6]int — РАЗНЫЕ типы.
// "int" — место для типа элементов: string, float64, bool...
// Все 5 элементов сразу равны нулевому значению (тут 0).

arr[0] = 42        // обращение по индексу, с нуля
fmt.Println(len(arr))
// len(x) ПРИНИМАЕТ массив/срез/строку/map, ВОЗВРАЩАЕТ int — количество элементов.
// Выведет: 5
\`\`\`

Массивы в Go используют редко — почти всегда берут срезы.

## Срез (slice) — динамический «массив»

\`\`\`go
nums := []int{1, 2, 3} // как массив, но БЕЗ числа в скобках — это срез

nums = append(nums, 4, 5)
// append ПРИНИМАЕТ: срез + один или несколько элементов его типа.
// ВОЗВРАЩАЕТ: НОВЫЙ срез с добавленными элементами.
// Обязательно присваивай результат обратно (nums = ...) —
// append может выделить новую память, и старая переменная её не увидит.
fmt.Println(nums)      // выведет: [1 2 3 4 5]
fmt.Println(nums[1:3]) // срез среза: элементы с индекса 1 ДО 3 (не включая) → [2 3]

make([]int, 3)    // создать срез длины 3 из нулей: [0 0 0]
make([]int, 0, 10) // длина 0, но память под 10 элементов уже выделена (capacity)
\`\`\`

## map — словарь ключ→значение

\`\`\`go
ages := map[string]int{ // map[типКлюча]типЗначения
	"Вова": 17,
	"Аня":  16,
}

ages["Петя"] = 18       // добавить/обновить
fmt.Println(ages["Вова"]) // выведет: 17
fmt.Println(ages["Нет"])  // выведет: 0 — для отсутствующего ключа вернётся нулевое значение!

age, ok := ages["Нет"]
// Обращение к map может возвращать ДВА значения: само значение и bool "ключ существует?".
// Это стандартный способ отличить "нет ключа" от "значение равно 0".
fmt.Println(age, ok) // выведет: 0 false

delete(ages, "Петя")
// delete ПРИНИМАЕТ: map и ключ. НИЧЕГО не возвращает. Удаляет пару из map.

for key, value := range ages { // range по map: первая переменная — КЛЮЧ, вторая — значение
	fmt.Println(key, value)
	// ВАЖНО: порядок обхода map СЛУЧАЙНЫЙ — Go специально его перемешивает.
}
\`\`\`
`,
          homework: `
1. Напиши программу: срез слов \`[]string{"go", "is", "go", "fun", "go"}\` → map со счётчиком, сколько раз встретилось каждое слово.
2. Объясни, почему \`nums = append(nums, x)\` требует присваивания, а \`ages["ключ"] = x\` для map — нет. (Подсказка: подумай, что происходит с памятью.)
`,
          cards: [
            { q: "Чем массив [5]int отличается от среза []int?", a: "У массива длина фиксирована и является частью типа; срез динамический" },
            { q: "Что принимает и возвращает append?", a: "Принимает срез + элементы, возвращает НОВЫЙ срез — результат нужно присвоить обратно" },
            { q: "Что вернёт map для отсутствующего ключа?", a: "Нулевое значение типа (0, \"\", false). Проверка существования: v, ok := m[key]" },
            { q: "Как удалить ключ из map?", a: "delete(m, key) — принимает map и ключ, ничего не возвращает" },
            { q: "В каком порядке range обходит map?", a: "В СЛУЧАЙНОМ — Go специально перемешивает порядок при каждом запуске" }
          ]
        },
        {
          id: "go-05",
          title: "Функции: аргументы, возврат нескольких значений, ошибки",
          theory: `
## Обычная функция

\`\`\`go
func add(a int, b int) int {
// func имя(аргумент тип, аргумент тип) типВозвращаемогоЗначения
// ПРИНИМАЕТ: два int. ВОЗВРАЩАЕТ: один int.
	return a + b
}

func add2(a, b int) int { // у соседних аргументов одного типа тип пишут один раз
	return a + b
}

sum := add(2, 3)
fmt.Println(sum) // выведет: 5
\`\`\`

## Несколько возвращаемых значений — фишка Go

\`\`\`go
func divmod(a, b int) (int, int) {
// ВОЗВРАЩАЕТ ДВА int: частное и остаток. Типы возврата — в скобках через запятую.
	return a / b, a % b
}

q, r := divmod(17, 5)
fmt.Println(q, r) // выведет: 3 2

q, _ := divmod(17, 5) // не нужен остаток — игнорируем через _
\`\`\`

## Ошибки: главный паттерн языка

В Go нет try/catch. Функция, которая может «сломаться», возвращает ошибку последним значением:

\`\`\`go
import (
	"errors"
	"fmt"
)

func safeDiv(a, b float64) (float64, error) {
// ВОЗВРАЩАЕТ: результат и error. error == nil означает "всё хорошо".
	if b == 0 {
		return 0, errors.New("деление на ноль")
		// errors.New ПРИНИМАЕТ строку, ВОЗВРАЩАЕТ значение типа error с этим текстом.
	}
	return a / b, nil // nil = "ошибки нет"
}

result, err := safeDiv(10, 0)
if err != nil { // этот блок ты будешь писать в Go тысячи раз — привыкай сразу
	fmt.Println("ошибка:", err) // выведет: ошибка: деление на ноль
	return
}
fmt.Println(result)
\`\`\`

Тот же паттерн у функций стандартной библиотеки:

\`\`\`go
data, err := os.ReadFile("config.txt")
// os.ReadFile ПРИНИМАЕТ: путь к файлу (string).
// ВОЗВРАЩАЕТ: ([]byte, error) — содержимое файла как срез байтов и ошибку.
// Если файла нет / нет прав — data будет nil, а err расскажет, что случилось.
if err != nil {
	fmt.Println("не смог прочитать:", err)
	return
}
fmt.Println(string(data)) // string(data) превращает []byte в строку
\`\`\`
`,
          homework: `
1. Напиши функцию \`minMax(nums []int) (int, int, error)\`: возвращает минимум и максимум, а для пустого среза — ошибку.
2. Напиши функцию, которая принимает срез строк и возвращает новый срез только из строк длиннее 3 символов.
3. Вопрос на подумать (ответ пришли мне): почему в Go ошибки возвращают значением, а не бросают исключение? Какие плюсы у такого подхода?
`,
          cards: [
            { q: "Как в Go функция сообщает об ошибке?", a: "Возвращает error последним значением; nil = ошибки нет. try/catch в Go нет" },
            { q: "Что принимает и возвращает os.ReadFile?", a: "Принимает путь (string), возвращает ([]byte, error) — содержимое файла и ошибку" },
            { q: "Как вернуть два значения из функции?", a: "Типы в скобках: func divmod(a, b int) (int, int) { return a/b, a%b }" },
            { q: "Что делает errors.New?", a: "Принимает строку, возвращает значение типа error с этим текстом" },
            { q: "Главный идиоматичный блок Go?", a: "if err != nil { ... } — проверка ошибки сразу после вызова" }
          ]
        }
      ]
    },
    {
      id: "structures-interfaces",
      title: "Модуль 3. Структуры и интерфейсы",
      lessons: [
        {
          id: "go-06",
          title: "Структуры: объявление, методы, встраивание, память",
          theory: `
## Объявление

\`\`\`go
type User struct {
	Name string
	Age  uint
	ID   int
}
// type ИмяТипа struct { поле тип; поле тип; ... }
// struct — составной тип: объединяет поля разных типов в один.

u := User{Name: "Вова", Age: 16}
// Незаполненные поля (ID) получают нулевое значение своего типа (тут 0).
\`\`\`

## Методы

\`\`\`go
type Player struct {
	Name string
	Game string
}

func (p Player) PrintFavGame() {
// (p Player) — ПОЛУЧАТЕЛЬ (receiver), не аргумент функции.
// Значит: метод PrintFavGame «привязан» к типу Player, вызывается как p.PrintFavGame().
// ПРИНИМАЕТ: ничего (кроме самого p). ВОЗВРАЩАЕТ: ничего.
	fmt.Printf("%s's favorite game is %s\\n", p.Name, p.Game)
}

func (p *Player) UpdateGame(newGame string) {
// Получатель ПО УКАЗАТЕЛЮ (*Player). Меняет ИСХОДНУЮ структуру, а не копию.
// ПРИНИМАЕТ: newGame (string). НИЧЕГО не возвращает.
	p.Game = newGame
}
\`\`\`

> Получатель по значению работает с КОПИЕЙ структуры — изменения внутри метода не затронут оригинал. Получатель по указателю меняет оригинал напрямую. Если хотя бы один метод типа меняет данные — обычно все методы этого типа делают по указателю, для единообразия.

## Встраивание (embedding) — «наследование» в Go

\`\`\`go
type Address struct {
	City    string
	Country string
}

type User struct {
	Username string
	Address  Address // ИМЕНОВАННОЕ поле-структура — это композиция
}
fmt.Println(user.Address.City) // доступ через имя поля

type Person struct {
	Name string
}
type Employee struct {
	Person // ВСТРОЕННОЕ поле — БЕЗ имени, это embedding
	Salary int
}
e := Employee{Person: Person{Name: "Аня"}, Salary: 1000}
fmt.Println(e.Name) // Name доступен НАПРЯМУЮ — поля встроенного типа "поднимаются" наружу
\`\`\`

> Это не настоящее наследование (методы не переопределяются полиморфно), а композиция — Go специально не имеет классического ООП-наследования.

## Память: выравнивание полей

Компилятор может добавлять «пустые» байты между полями, чтобы адрес каждого поля был кратен размеру его типа:

\`\`\`go
type Example struct {
	a int8  // 1 байт (+ 3 байта паддинга для выравнивания под b)
	b int32 // 4 байта — самый большой элемент
	c int8  // 1 байт (+ 3 байта паддинга для кратности размера структуры)
}
fmt.Println(unsafe.Sizeof(Example{})) // выведет: 12, хотя поля сами весят 1+4+1=6

type Optimized struct {
	b int32 // 4 байта
	a int8  // 1 байт
	c int8  // 1 байт (+ 2 байта паддинга)
}
fmt.Println(unsafe.Sizeof(Optimized{})) // выведет: 8
\`\`\`

> Правило economии памяти: располагай поля от БОЛЬШИХ к МАЛЕНЬКИМ по размеру типа.

## Сравнение структур

Структуры можно сравнивать через \`==\`, ЕСЛИ все поля сравнимы (никаких срезов, map, функций внутри). Если есть несравнимые поля — нужен \`reflect.DeepEqual\`.
`,
          homework: `
1. Напиши структуру \`Player\` с полем \`Score int\` и двумя методами: один с получателем по значению пытается увеличить Score, другой — с получателем по указателю реально его увеличивает. Выведи Score до и после обоих вызовов и объясни разницу.
2. Есть структура \`struct { a int8; b int64; c int8 }\`. Посчитай её реальный размер через \`unsafe.Sizeof\`, затем переставь поля для экономии памяти и сравни результат.
3. Объясни своими словами: чем embedding отличается от именованного вложенного поля (composition)? Приведи свой пример на 3-4 строки кода.
`,
          cards: [
            { q: "Чем получатель по указателю (*Player) отличается от получателя по значению?", a: "По указателю можно менять исходную структуру. По значению — работаем с копией, изменения не сохраняются" },
            { q: "Что такое embedding (встраивание) структур?", a: "Включение одной структуры в другую БЕЗ имени поля — поля и методы встроенного типа становятся доступны у внешнего напрямую. Это композиция, а не наследование" },
            { q: "Как посмотреть реальный размер структуры в байтах?", a: "unsafe.Sizeof(MyStruct{})" },
            { q: "Почему struct{a int8; b int64; c int8} весит 24 байта, а не 10?", a: "Выравнивание: int64 требует кратности 8, поэтому вокруг int8-полей компилятор добавляет пустые байты-паддинги" },
            { q: "Как сэкономить память в структуре через порядок полей?", a: "Располагать поля от БОЛЬШИХ по размеру типа к МЕНЬШИМ" },
            { q: "Когда структуры можно сравнивать оператором ==?", a: "Если все поля сравнимы (нет срезов, map, функций) — иначе нужен reflect.DeepEqual" }
          ]
        },
        {
          id: "go-07",
          title: "JSON: Marshal, Unmarshal и struct tags",
          theory: `
## Struct tag — метаданные поля

\`\`\`go
type User struct {
	Name  string \`json:"name"\`
	Age   int    \`json:"age"\`
	Email string \`json:"email"\`
}
// Тег в обратных кавычках после типа поля. json:"name" говорит пакету
// encoding/json: "в JSON это поле называть name (с маленькой буквы)".
\`\`\`

## Marshal — Go-структура → JSON

\`\`\`go
data, err := json.Marshal(user)
// ПРИНИМАЕТ: любое Go-значение (обычно структуру).
// ВОЗВРАЩАЕТ: ([]byte, error) — JSON как срез байт и ошибку.
if err != nil {
	log.Fatal(err)
}
fmt.Println(string(data)) // выведет: {"name":"Alice","age":30,"email":"alice@example.com"}

// Для читаемого вывода с отступами:
data, err = json.MarshalIndent(user, "", "  ")
// ТРЕТИЙ аргумент — строка отступа для каждого уровня вложенности.
\`\`\`

## Unmarshal — JSON → Go-структура

\`\`\`go
jsonData := []byte(\`{"name":"Bob","age":25,"email":"bob@example.com"}\`)
var user User
err := json.Unmarshal(jsonData, &user)
// ПРИНИМАЕТ: срез байт JSON и УКАЗАТЕЛЬ на структуру (&user, не user!).
// Указатель нужен, чтобы функция могла ЗАПОЛНИТЬ существующую переменную.
// ВОЗВРАЩАЕТ: error (nil, если всё распарсилось).
if err != nil {
	log.Fatal(err)
}
fmt.Printf("Name: %s, Age: %d\\n", user.Name, user.Age)
\`\`\`

## Полезные фишки тегов

\`\`\`go
type User struct {
	Name     string \`json:"name"\`
	Password string \`json:"-"\`             // "-" исключает поле из JSON ПОЛНОСТЬЮ
	Age      int    \`json:"age,omitempty"\`  // omitempty пропускает поле, если оно = нулевому значению
}
\`\`\`

> Ловушка \`omitempty\`: он не отличает «явный ноль» от «поле не задано» — оба случая (\`0\`, \`""\`, \`false\`) будут исключены из JSON. Если это важно различать — делай поле указателем (\`*int\`): у указателя нулевое значение \`nil\`, а \`0\` — это уже осознанно установленное значение.

## Вложенные структуры

Работают без проблем — теги применяются на любом уровне вложенности:

\`\`\`go
type Address struct {
	City string \`json:"city"\`
}
type User struct {
	Name    string  \`json:"name"\`
	Address Address \`json:"address"\`
}
// Marshal даст: {"name":"Alice","address":{"city":"Boston"}}
\`\`\`

## Непредвиденные поля

Если в JSON есть поля, которых нет в структуре, Go их молча проигнорирует. Чтобы их получить:

\`\`\`go
type User struct {
	Name  string                 \`json:"name"\`
	Extra map[string]interface{} \`json:"extra"\` // сюда попадёт "всё остальное", если явно настроить логику
}
\`\`\`
`,
          homework: `
1. Опиши структуру \`Product\` (Name string, Price float64, InStock bool) с json-тегами, замаршаль в JSON и выведи результат через \`json.MarshalIndent\`.
2. Возьми строку JSON вида \`{"name":"Клавиатура","price":2500,"inStock":false}\` и распарси её в структуру через \`json.Unmarshal\`. Не забудь проверить \`err\`.
3. Продемонстрируй ловушку \`omitempty\`: покажи, что структура с \`Age int \`json:"age,omitempty"\`\` и явным \`Age: 0\` даст в JSON ТО ЖЕ САМОЕ, что и структура без Age вообще. Потом реши эту проблему через \`*int\`.
`,
          cards: [
            { q: "Что принимает и возвращает json.Marshal?", a: "Принимает Go-значение (обычно структуру), возвращает ([]byte, error) — JSON и ошибку" },
            { q: "Что нужно передать вторым аргументом в json.Unmarshal и почему?", a: "УКАЗАТЕЛЬ на структуру (&user) — чтобы функция могла заполнить существующую переменную, а не вернуть новую" },
            { q: "Как убрать поле из JSON при маршалинге?", a: "Тег json:\"-\"" },
            { q: "В чём ловушка omitempty?", a: "Не отличает «явный ноль» от «поле не задано» — оба исключаются из JSON. Для различения используют указатель (*int)" },
            { q: "Что произойдёт при Unmarshal JSON-поля, не объявленного в структуре?", a: "Go по умолчанию его проигнорирует; чтобы получить — использовать map[string]interface{}" }
          ]
        },
        {
          id: "go-08",
          title: "Интерфейсы: утиная типизация, nil-ловушка, type assertion",
          theory: `
## Что такое интерфейс

Интерфейс — тип, который описывает НАБОР МЕТОДОВ (без реализации). Любой тип, у которого ЕСТЬ все эти методы, автоматически ему удовлетворяет — явно писать «этот тип реализует этот интерфейс» не нужно (утиная типизация: «если крякает как утка — значит утка»).

\`\`\`go
type Swimmer interface {
	Swim()
}
// ПРИНИМАЕТ: ничего (это просто описание метода). Сам интерфейс не выполняет код.

func GoToCup(s Swimmer) {
// ПРИНИМАЕТ: любое значение, у которого есть метод Swim(). ВОЗВРАЩАЕТ: ничего.
	s.Swim()
}

type Human struct{}
func (h Human) Swim() { fmt.Println("Человек плывёт брассом") }

type Car struct{}
// у Car нет метода Swim()

GoToCup(Human{}) // ✅ работает — у Human есть Swim()
GoToCup(Car{})   // ❌ ошибка компиляции — у Car нет Swim()
\`\`\`

## Зачем нужны

Без интерфейса функция жёстко привязана к одному типу. С интерфейсом — работает с любым типом, у которого есть нужный метод:

\`\`\`go
type Speaker interface {
	Speak() string
}
type Dog struct{}
func (d Dog) Speak() string { return "woof" }
type Cat struct{}
func (c Cat) Speak() string { return "meow" }

func PrintSound(s Speaker) {
// ПРИНИМАЕТ: любой Speaker (Dog, Cat, или что угодно ещё с методом Speak).
	fmt.Println(s.Speak())
}
PrintSound(Dog{}) // woof
PrintSound(Cat{}) // meow
\`\`\`

## Пустой интерфейс и any

\`interface{}\` (или \`any\` начиная с Go 1.18) — интерфейс БЕЗ методов, ему удовлетворяет ЛЮБОЙ тип. Используется, когда нужно принять «что угодно».

## Type assertion — узнать конкретный тип за интерфейсом

\`\`\`go
var i interface{} = "hello"
n, ok := i.(int)
// i.(Type) ПРИНИМАЕТ проверяемый тип, ВОЗВРАЩАЕТ (значение, bool).
// ok == false, если внутри i на самом деле не int.
fmt.Println(n, ok) // выведет: 0 false

n2 := i.(int) // БЕЗ ok — если тип не совпал, будет ПАНИКА
\`\`\`

## Главная ловушка интерфейсов: nil ≠ nil

\`\`\`go
var d *Dog = nil
var s Speaker = d
fmt.Println(s == nil) // выведет: false !
// Потому что переменная интерфейса хранит ПАРУ (тип, значение): тут (*Dog, nil).
// Раз тип не nil, весь интерфейс не считается nil, даже если значение внутри — nil.
\`\`\`

Отсюда правило для функций, возвращающих \`error\`:

\`\`\`go
func GetError() error {
	if somethingWrong {
		return &MyError{} // конкретная ошибка
	}
	return nil // ВСЕГДА возвращай "голый" nil, а не (*MyError)(nil)
}
\`\`\`
`,
          homework: `
1. Опиши интерфейс \`Shape\` с методом \`Area() float64\`, реализуй его для \`Circle\` и \`Rectangle\`, напиши функцию \`PrintArea(s Shape)\` и вызови её для обоих типов.
2. Заведи переменную \`var x interface{} = 42\` и через type assertion с \`ok\` проверь, является ли она \`string\` и \`int\` — покажи оба результата.
3. Объясни своими словами (без подглядывания), почему \`var d *Dog = nil; var s Speaker = d\` даёт \`s == nil\` равным \`false\`. Это частый вопрос на собеседованиях.
`,
          cards: [
            { q: "Что такое интерфейс в Go?", a: "Тип, определяющий набор методов; любой тип, реализующий все методы, автоматически ему удовлетворяет (утиная типизация)" },
            { q: "Что хранит переменная интерфейса внутри?", a: "Пару (тип, значение) — динамический тип и само значение/указатель" },
            { q: "Что выведет var d *Dog = nil; var s Speaker = d; fmt.Println(s == nil)?", a: "false — потому что s хранит пару (*Dog, nil), а не просто nil" },
            { q: "Что такое настоящий nil-интерфейс?", a: "Интерфейс, у которого И тип, И значение равны nil одновременно. Только такой равен nil" },
            { q: "Как выполнить безопасный type assertion?", a: "val, ok := i.(ConcreteType) — при ok=false паники не будет" },
            { q: "Что будет при type assertion без ok на неверный тип?", a: "Паника" },
            { q: "Что такое пустой интерфейс interface{} / any?", a: "Интерфейс без методов, которому удовлетворяет абсолютно любой тип" }
          ]
        },
        {
          id: "go-09",
          title: "Comma-ok идиома: четыре случая применения",
          theory: `
## Общий синтаксис

\`\`\`go
value, ok := expression // value — результат, ok (bool) — «получилось ли»
value, err := expression // тот же приём, но вторая переменная — error вместо bool
\`\`\`

Это НЕ одна конструкция языка, а общий приём, применяемый в четырёх разных ситуациях.

## 1. Ошибка при возврате из функции

\`\`\`go
func isEven(n int) (bool, error) {
	if n&1 == 1 { // n&1 — побитовое И с 1: равно 1, если n нечётное. Быстрее, чем n%2
		return false, fmt.Errorf("%d — нечётное число", n)
	}
	return true, nil
}
ok, err := isEven(3)
if !ok {
	fmt.Println(err) // выведет: 3 — нечётное число
}
\`\`\`

## 2. Проверка ключа в map (уже видел в go-04, вот почему это важно)

\`\`\`go
myMap := map[string]int{"apple": 5}
value, ok := myMap["cherry"]
// value == 0 (нулевое значение int), ok == false — ключа нет.
// БЕЗ ok отличить "значение 0" от "ключа нет" невозможно!
\`\`\`

## 3. Type assertion интерфейса (уже видел в go-08)

\`\`\`go
var i interface{} = "hello"
n, ok := i.(int) // ok == false, i — не int
\`\`\`

## 4. Проверка, закрыт ли канал

\`\`\`go
ch := make(chan int)
go func() {
	ch <- 1
	close(ch)
}()
for {
	x, ok := <-ch
	// ok == true, пока есть данные для чтения.
	// ok == false — канал ЗАКРЫТ и ПУСТ (больше значений не будет).
	if !ok {
		break
	}
	fmt.Println(x)
}
\`\`\`

> Во всех четырёх случаях идея одна: получить «результат + флаг успешности» одним выражением, без исключений (которых в Go нет) и без риска перепутать «нулевое значение» с «операция не удалась».
`,
          homework: `
1. Перепиши функцию \`isEven\` из урока так, чтобы вместо побитового \`n&1\` использовать \`n%2\`, и объясни, почему результат тот же самое, но \`&1\` считается идиоматичнее в Go для чётности.
2. Смоделируй баг: получи значение из map БЕЗ \`ok\` для ключа, которого нет, и покажи, как легко перепутать это с реальным нулём. Потом исправь через comma-ok.
3. Напиши горутину, которая шлёт 3 числа в канал и закрывает его, а в \`main\` вычитывай через \`for { x, ok := <-ch; ... }\`, пока \`ok\` не станет \`false\`.
`,
          cards: [
            { q: "В каких 4 случаях используется comma-ok идиома?", a: "1) ошибка в return функции, 2) проверка ключа в map, 3) type assertion интерфейса, 4) проверка, закрыт ли канал" },
            { q: "Как отличить «значение 0» от «ключа нет» в map?", a: "Через comma-ok: value, ok := m[key]; если ok==false — ключа нет вообще" },
            { q: "Что вернёт x, ok := <-ch для закрытого и опустошённого канала?", a: "x — нулевое значение типа канала, ok == false" },
            { q: "Что означает n&1 == 1 для целого n?", a: "Проверка на нечётность через побитовое И — эквивалент n%2==1, но быстрее" }
          ]
        }
      ]
    },
    {
      id: "concurrency",
      title: "Модуль 4. Конкурентность",
      lessons: [
        {
          id: "go-10",
          title: "Горутины и sync.WaitGroup",
          theory: `
## Горутина — лёгкий поток

\`\`\`go
func PrintA() {
	fmt.Println("A")
}
func main() {
	go PrintA()       // go func — ЗАПУСКАЕТ функцию в отдельной горутине, не дожидаясь её
	fmt.Println("B")  // main продолжает выполняться сразу
}
// Порядок вывода НЕ ГАРАНТИРОВАН: может быть "A\\nB", "B\\nA" или ДАЖЕ ТОЛЬКО "B"
\`\`\`

> Ключевая проблема: когда \`main()\` завершается, ВСЯ программа завершается — все горутины принудительно убиваются, даже недоделанные.

\`\`\`go
go func() { ... }()
// ЭТО ПРАВИЛЬНО: круглые скобки в конце ВЫЗЫВАЮТ функцию — go запускает именно вызов.
go func() { ... }
// ЭТО ОШИБКА: без () это просто ОБЪЯВЛЕНИЕ функции, которое никогда не выполнится.
\`\`\`

## sync.WaitGroup — дождаться завершения горутин

\`\`\`go
var wg sync.WaitGroup

func PrintA() {
	defer wg.Done() // ВЫЗЫВАЕТСЯ в конце PrintA, что бы ни случилось — уменьшает счётчик на 1
	fmt.Println("A")
}

func main() {
	wg.Add(1) // ПРИНИМАЕТ delta int — говорим "будет ещё 1 незавершённая задача"
	go PrintA()
	wg.Wait() // БЛОКИРУЕТ main, пока счётчик не станет 0
}
\`\`\`

**Три метода WaitGroup:**
- \`Add(delta int)\` — увеличить счётчик задач
- \`Done()\` — уменьшить счётчик на 1 (эквивалент \`Add(-1)\`)
- \`Wait()\` — заблокироваться, пока счётчик не станет 0

> ⚠️ \`sync.WaitGroup\` НЕЛЬЗЯ копировать (передавать по значению) — только по указателю (\`*sync.WaitGroup\`), иначе разные копии будут считать независимо друг от друга.
`,
          homework: `
1. Запусти 5 горутин, каждая печатает "worker N" (N от 1 до 5), с правильным использованием \`sync.WaitGroup\`, чтобы \`main\` гарантированно дождался всех.
2. Убери \`wg.Wait()\` из своего решения и запусти несколько раз — покажи, что вывод теперь непредсказуем (может быть пустым или неполным).
3. Объясни своими словами, почему \`go func(){}()\` работает, а \`go func(){}\` — нет.
`,
          cards: [
            { q: "Что такое горутина и как её правильно запустить?", a: "Легковесный поток, управляемый рантаймом Go; запускается: go func() {...}() — с круглыми скобками, вызывающими функцию" },
            { q: "Что произойдёт, если main() завершится, пока горутина ещё работает?", a: "Все горутины принудительно убиваются, программа завершается немедленно" },
            { q: "Как дождаться завершения нескольких горутин?", a: "sync.WaitGroup: Add(n) перед запуском, defer Done() внутри горутины, Wait() в main блокирует до счётчика 0" },
            { q: "Можно ли копировать sync.WaitGroup (передавать по значению)?", a: "Нет — всегда передавайте указатель *sync.WaitGroup" },
            { q: "Что выведет go PrintA(); fmt.Println(\"B\") без WaitGroup?", a: "Порядок не определён — может быть «AB», «BA» или только «B»" }
          ]
        },
        {
          id: "go-11",
          title: "Каналы, select и таймауты",
          theory: `
## Каналы — «трубы» между горутинами

\`\`\`go
ch := make(chan int) // make(chan Тип) — создать НЕбуферизированный канал
ch <- 5               // отправить значение (блокирует, пока кто-то не прочитает)
x := <-ch              // получить значение (блокирует, пока кто-то не отправит)
\`\`\`

> Небуферизированный канал требует ОДНОВРЕМЕННОЙ готовности отправителя и получателя. \`ch <- 5\` без читателя в другой горутине — мгновенный **deadlock**.

## Буферизированные каналы

\`\`\`go
ch := make(chan int, 2) // ВТОРОЙ аргумент — размер буфера
ch <- 1 // не блокирует — есть место в буфере
ch <- 2 // не блокирует — буфер заполнен
ch <- 3 // БЛОКИРУЕТ — буфер полон, ждём, пока кто-то прочитает
\`\`\`

## Закрытие канала — 4 жёстких правила

\`\`\`go
close(ch)
\`\`\`

1. Закрывать канал должен ОТПРАВИТЕЛЬ, не получатель.
2. Отправка в закрытый канал → **паника**.
3. Чтение из закрытого канала → РАЗРЕШЕНО, вернёт нулевое значение.
4. Повторное \`close()\` уже закрытого канала → **паника**.

\`\`\`go
ch := make(chan int)
go func() {
	for i := 0; i < 5; i++ {
		ch <- i
	}
	close(ch) // обязательно, иначе range ниже зависнет НАВСЕГДА
}()
for v := range ch { // range по каналу завершается, когда канал ЗАКРЫТ и ПУСТ
	fmt.Println(v)
}
\`\`\`

## select — «switch» для каналов

\`\`\`go
select {
case msg := <-ch1:
	fmt.Println(msg)
case msg := <-ch2:
	fmt.Println(msg)
}
// ПРИНИМАЕТ: несколько case с операциями над каналами.
// Выполняет ПЕРВЫЙ готовый case. Если готовы несколько — выбирает СЛУЧАЙНЫЙ (без "голодания").
// Без default БЛОКИРУЕТСЯ, пока хотя бы один case не станет готов.
\`\`\`

Неблокирующий select — добавь \`default\`:

\`\`\`go
select {
case v := <-ch:
	fmt.Println(v)
default:
	fmt.Println("нет данных прямо сейчас")
}
\`\`\`

Таймаут — частый вопрос на собеседованиях:

\`\`\`go
select {
case msg := <-ch:
	fmt.Println(msg)
case <-time.After(1 * time.Second):
// time.After(d) ВОЗВРАЩАЕТ канал, в который придёт значение через d — удобный способ таймаута
	fmt.Println("timeout")
}
\`\`\`
`,
          homework: `
1. Запусти две горутины: одна через \`time.Sleep\` + отправку в \`ch1\` эмулирует «быстрый» ответ (1 секунда), другая — «медленный» в \`ch2\` (2 секунды). Через \`select\` выведи, какой ответ пришёл первым.
2. К предыдущему коду добавь \`case <-time.After(500*time.Millisecond)\` и объясни, что изменится в выводе.
3. Объясни своими словами, почему \`ch := make(chan int); ch <- 5\` (без горутины-читателя) — это мгновенный deadlock, а не просто ошибка.
`,
          cards: [
            { q: "Что произойдёт при отправке в закрытый канал?", a: "Паника" },
            { q: "Что произойдёт при чтении из закрытого канала?", a: "Разрешено — возвращает нулевое значение типа канала (не паника)" },
            { q: "Кто должен закрывать канал — отправитель или получатель?", a: "Только отправитель" },
            { q: "Как безопасно проверить, закрыт ли канал?", a: "x, ok := <-ch; ok == false означает, что канал закрыт и пуст" },
            { q: "Что делает select, если ни один case не готов и нет default?", a: "Блокируется, пока хотя бы один case не станет готов" },
            { q: "Как сделать неблокирующий select?", a: "Добавить default" },
            { q: "Как сделать таймаут для операции с каналом?", a: "case <-time.After(duration): внутри select" },
            { q: "Почему for v := range ch зависает навсегда, если канал не закрыт?", a: "range по каналу завершается только когда канал закрыт — иначе бесконечно ждёт новые значения" }
          ]
        },
        {
          id: "go-12",
          title: "Mutex и паттерны конкурентности (fan-in/fan-out)",
          theory: `
## Mutex — защита общих данных

\`\`\`go
var mu sync.Mutex
var x int

mu.Lock()   // захватить мьютекс — другие горутины ждут
x++         // безопасно менять общие данные
mu.Unlock() // освободить — следующая горутина может зайти
\`\`\`

Идиоматично сразу писать \`defer\`, чтобы Unlock точно случился при любом выходе из функции (даже при панике):

\`\`\`go
func increment() {
	mu.Lock()
	defer mu.Unlock()
	x++
}
\`\`\`

**Ловушки, которые реально спрашивают на собеседованиях:**

\`\`\`go
mu.Lock()
mu.Lock() // ВТОРОЙ Lock() в той же горутине без Unlock() между ними → DEADLOCK

mu.Unlock() // Unlock() НЕЗАХВАЧЕННОГО мьютекса → PANIC: unlock of unlocked mutex
\`\`\`

## Mutex НЕ гарантирует завершения горутин!

Частая путаница: Mutex защищает ДАННЫЕ от гонки, но НЕ ждёт, пока горутины закончат работу — для этого всё равно нужен \`sync.WaitGroup\`.

\`\`\`go
var mu sync.Mutex
x := 0
for i := 0; i < 1000; i++ {
	go func() {
		mu.Lock()
		x++
		mu.Unlock()
	}()
}
fmt.Println(x) // выведет ЛЮБОЕ число от 0 до 1000!
// Mutex гарантирует, что x++ не сломается от гонки данных,
// НО main не дожидается завершения горутин — они могут ещё не успеть выполниться.
// Нужен ЕЩЁ и WaitGroup, чтобы дождаться всех 1000.
\`\`\`

## Правила и паттерны конкурентного кода

- **fan-in**: несколько каналов объединяются в один — горутина читает из \`ch1\` и \`ch2\`, пишет в общий \`out\`.
- **fan-out**: один канал заданий раздаёт данные нескольким воркерам — каждый воркер читает из общего канала.
- **worker pool** — частный случай fan-out: воркеры запускаются один раз и ждут задания в цикле.
- Правило порядка: сначала запускай воркеров (горутины), потом отправляй данные в канал.
- Буферизированные каналы «прячут» плохую синхронизацию — ошибки (забытый \`close\`) проявятся позже и их сложнее найти.
`,
          homework: `
1. Возьми пример из урока с гонкой (\`x++\` в 1000 горутинах), добавь \`sync.WaitGroup\`, чтобы \`main\` гарантированно дождался всех горутин и вывел РОВНО 1000.
2. Объясни своими словами разницу между тем, что защищает Mutex, и тем, что защищает WaitGroup — это разные задачи.
3. Реализуй простой fan-out: канал \`jobs\` с 10 числами, 3 воркера-горутины читают из него и печатают квадрат числа. Не забудь закрыть \`jobs\` после отправки и дождаться воркеров через WaitGroup.
`,
          cards: [
            { q: "Что делает Mutex и зачем?", a: "Защищает общую память (данные) от одновременного изменения несколькими горутинами" },
            { q: "Что произойдёт при повторном Lock() без Unlock() в той же горутине?", a: "Deadlock — второй Lock() ждёт освобождения бесконечно" },
            { q: "Что произойдёт при Unlock() незахваченного мьютекса?", a: "Panic: unlock of unlocked mutex" },
            { q: "Гарантирует ли Mutex, что все горутины завершатся до продолжения main?", a: "Нет — Mutex защищает данные от гонки, но не ждёт завершения. Для ожидания нужен WaitGroup" },
            { q: "Зачем сразу после Lock() пишут defer mu.Unlock()?", a: "Чтобы гарантированно освободить мьютекс при любом выходе из функции, включая панику" },
            { q: "Что такое fan-in и fan-out?", a: "fan-in — объединение нескольких каналов в один; fan-out — один канал раздаёт задачи нескольким воркерам" },
            { q: "Правило порядка запуска в конкурентном коде?", a: "Сначала стартуем воркеров (горутины), потом отправляем данные" }
          ]
        }
      ]
    },
    {
      id: "data-structures-algo",
      title: "Модуль 5. Структуры данных и алгоритмы на Go",
      lessons: [
        {
          id: "go-13",
          title: "Деревья: DFS и три порядка обхода",
          theory: `
## TreeNode — узел дерева

\`\`\`go
type TreeNode struct {
	Val   int
	Left  *TreeNode // УКАЗАТЕЛЬ на левого ребёнка — храним ССЫЛКУ, а не копию узла
	Right *TreeNode
}
\`\`\`

Возьмём конкретное дерево для примеров ниже:

\`\`\`
        1
       / \\
      2   3
     / \\
    4   5
\`\`\`

## DFS (Depth First Search) — обход в глубину

Три классических порядка, отличающихся только тем, КОГДА обрабатывается сам узел относительно детей:

| Порядок | Формула | Результат для дерева выше |
|---|---|---|
| Preorder | Root → Left → Right | 1, 2, 4, 5, 3 |
| Inorder | Left → Root → Right | 4, 2, 5, 1, 3 |
| Postorder | Left → Right → Root | 4, 5, 2, 3, 1 |

## Базовый шаблон рекурсивного DFS

\`\`\`go
func preorder(root *TreeNode) {
	if root == nil { // БАЗОВЫЙ СЛУЧАЙ: пустое поддерево — завершаем рекурсию
		return
	}
	fmt.Println(root.Val) // 1. обработать текущий узел
	preorder(root.Left)   // 2. полностью обойти левое поддерево
	preorder(root.Right)  // 3. полностью обойти правое поддерево
}
// Шаблон один и тот же для всех трёх порядков — меняется только МЕСТО строки с обработкой узла.
\`\`\`

> Почему это работает без явного стека: РЕКУРСИЯ САМА использует стек вызовов функций. Память растёт пропорционально высоте дерева \`h\`, то есть DFS рекурсией занимает **O(h)** памяти, а не O(n).

## Подсчёт узлов vs сумма значений — не перепутай формулу

\`\`\`go
func count(root *TreeNode) int {
	if root == nil {
		return 0
	}
	return 1 + count(root.Left) + count(root.Right)
	// "1" — это ТЕКУЩИЙ узел, плюс количество узлов в обоих поддеревьях
}

func sum(root *TreeNode) int {
	if root == nil {
		return 0
	}
	return root.Val + sum(root.Left) + sum(root.Right)
	// тут вместо "1" — ЗНАЧЕНИЕ текущего узла
}
\`\`\`

> Ловушка: \`root.Left.Val\` — это значение ОДНОГО конкретного узла (левого ребёнка). А \`sum(root.Left)\` — результат обхода ВСЕГО левого поддерева. Их легко перепутать в рекурсии.
`,
          homework: `
1. Построй дерево из урока (\`1\` с детьми \`2,3\`; у \`2\` дети \`4,5\`) в коде и реализуй \`preorder\`, \`inorder\`, \`postorder\` — сверь вывод с таблицей из урока.
2. Напиши \`count(root *TreeNode) int\` и \`sum(root *TreeNode) int\` для этого дерева и проверь результат руками.
3. Объясни своими словами, почему рекурсивный DFS занимает O(h) памяти, а не O(n), где n — общее число узлов.
`,
          cards: [
            { q: "Что означает DFS?", a: "Depth First Search — обход дерева/графа в глубину" },
            { q: "Три порядка обхода дерева и их формулы?", a: "Preorder: Root→Left→Right; Inorder: Left→Root→Right; Postorder: Left→Right→Root" },
            { q: "Базовый шаблон рекурсивного DFS по дереву?", a: "Проверка на nil → обработка текущего узла → рекурсивный вызов для левого и правого детей" },
            { q: "Почему рекурсивный DFS использует O(h) памяти, где h — высота дерева?", a: "Потому что вызовы функций хранятся в стеке вызовов — глубина стека равна высоте дерева" },
            { q: "Чем отличается подсчёт узлов дерева от суммы значений узлов?", a: "Подсчёт: 1 + count(left) + count(right). Сумма: root.Val + sum(left) + sum(right)" },
            { q: "Что возвращает count(nil)?", a: "0 — базовый случай рекурсии для пустого дерева" },
            { q: "Preorder/Inorder/Postorder для дерева 1(2(4,5),3)?", a: "Preorder: 1,2,4,5,3; Inorder: 4,2,5,1,3; Postorder: 4,5,2,3,1" }
          ]
        },
        {
          id: "go-14",
          title: "Связные списки: удаление узла",
          theory: `
## Односвязный список

\`\`\`go
type ListNode struct {
	Val  int
	Next *ListNode // указатель на следующий узел; nil у последнего
}
\`\`\`

## Удаление узла, когда есть указатель на ПРЕДЫДУЩИЙ

Стандартный случай: перенаправляем \`Next\` предыдущего узла через удаляемый — на следующий за ним.

\`\`\`go
func deleteNext(prev *ListNode) {
	// ПРИНИМАЕТ: узел ПЕРЕД тем, который надо удалить. НИЧЕГО не возвращает.
	if prev == nil || prev.Next == nil {
		return
	}
	prev.Next = prev.Next.Next // "перепрыгиваем" через удаляемый узел
}
\`\`\`

## Ловушка: удаление, если есть указатель ТОЛЬКО на сам узел

Иногда (например, в задачах на LeetCode) доступа к предыдущему узлу нет — только к самому удаляемому \`node\`. Прямого способа «дотянуться назад» в односвязном списке НЕТ. Трюк:

\`\`\`go
func deleteNode(node *ListNode) {
	// ПРИНИМАЕТ: узел, который нужно удалить (доступа к prev нет!).
	node.Val = node.Next.Val   // 1. копируем данные из СЛЕДУЮЩЕГО узла в текущий
	node.Next = node.Next.Next // 2. перепрыгиваем через следующий узел
	// По факту текущий узел "притворился" следующим, а настоящий следующий выпал из цепочки
}
\`\`\`

> Ловушка этого трюка: он НЕ РАБОТАЕТ для последнего узла списка — у него нет \`Next\`, скопировать данные неоткуда. В такой ситуации без указателя на предыдущий узел удаление в принципе невозможно (в односвязном списке нельзя пойти "назад").
`,
          homework: `
1. Реализуй \`ListNode\` и функцию \`deleteByValue(head *ListNode, val int) *ListNode\`, которая удаляет первый узел с данным значением (с учётом того, что может понадобиться удалить сам \`head\`).
2. Реализуй трюк \`deleteNode\` из урока (удаление по указателю только на сам узел) и проверь на списке \`1→2→3→4\`, удаляя узел со значением \`3\`.
3. Объясни своими словами, почему трюк из пункта 2 ломается на последнем узле списка.
`,
          cards: [
            { q: "Как удалить узел из односвязного списка, если известен предыдущий узел?", a: "Перенаправить его Next через удаляемый узел на следующий: prev.Next = node.Next" },
            { q: "Как удалить узел, если есть указатель ТОЛЬКО на него (без доступа к предыдущему)?", a: "Скопировать данные из следующего узла в текущий, затем перенаправить Next через следующий узел" },
            { q: "Почему этот трюк не работает для последнего узла списка?", a: "У последнего узла нет Next — копировать данные неоткуда, а без доступа к предыдущему в одну сторону не развернуться" }
          ]
        },
        {
          id: "go-15",
          title: "Бинарный поиск: sort.SearchInts и подсчёт границ",
          theory: `
## sort.SearchInts

\`\`\`go
import "sort"

arr := []int{1, 3, 3, 5, 8, 8, 8, 10}
idx := sort.SearchInts(arr, 5)
// ПРИНИМАЕТ: ОТСОРТИРОВАННЫЙ []int и искомое значение x.
// ВОЗВРАЩАЕТ: индекс ПЕРВОЙ позиции, где элемент >= x.
// Если такого элемента нет (x больше всех) — вернёт len(arr).
fmt.Println(idx) // выведет: 3 (arr[3] == 5)
\`\`\`

Внутри — классический бинарный поиск за O(log n), но писать его руками не нужно — стандартная библиотека уже это сделала.

## Как считать количество элементов в диапазоне

Три готовые формулы, которые стоит помнить наизусть — они выводятся из одного и того же \`SearchInts\`:

\`\`\`go
// количество элементов ≤ x:
countLE := sort.SearchInts(arr, x+1)

// количество элементов < x:
countLT := sort.SearchInts(arr, x)

// количество элементов ≥ x:
countGE := len(arr) - sort.SearchInts(arr, x)
\`\`\`

Пример: сколько элементов в \`arr\` строго больше 5?

\`\`\`go
countGT := len(arr) - sort.SearchInts(arr, 6) // "≥ 6" эквивалентно "> 5" для целых чисел
\`\`\`

> Приём этих трёх формул часто нужен в задачах, где просят «количество элементов не превышающих X» — типичная формулировка в олимпиадных и ЕГЭ-подобных задачах на массивы.
`,
          homework: `
1. Дан отсортированный срез \`[]int{2, 4, 4, 4, 7, 9, 9, 12}\`. Без запуска кода в уме посчитай: сколько элементов ≤ 4? Сколько < 4? Сколько ≥ 9? Потом проверь код через \`sort.SearchInts\`.
2. Напиши функцию \`countInRange(arr []int, low, high int) int\`, которая считает элементы в диапазоне \`[low, high]\` (включительно), используя ДВА вызова \`sort.SearchInts\`.
3. Что вернёт \`sort.SearchInts(arr, 100)\`, если в \`arr\` максимум 12? Объясни, почему именно так, и как это использовать в формуле "количество ≥ x".
`,
          cards: [
            { q: "Что принимает и возвращает sort.SearchInts(arr, x)?", a: "Принимает отсортированный []int и искомое x; возвращает индекс первой позиции, где элемент >= x (если такого нет — len(arr))" },
            { q: "Как посчитать количество элементов ≤ x в отсортированном срезе?", a: "sort.SearchInts(arr, x+1)" },
            { q: "Как посчитать количество элементов < x?", a: "sort.SearchInts(arr, x)" },
            { q: "Как посчитать количество элементов ≥ x?", a: "len(arr) - sort.SearchInts(arr, x)" }
          ]
        },
        {
          id: "go-16",
          title: "Sliding window (скользящее окно)",
          theory: `
## Идея

Sliding window — техника для задач на НЕПРЕРЫВНЫЙ подмассив/подстроку с каким-то условием («без повторов», «сумма ≤ K» и т.п.). Вместо перебора всех подмассивов (O(n²) или хуже), двигаем «окно» \`[left, right]\` по одному разу каждый указатель — получаем O(n).

**Алгоритм:**
1. Двигай \`right\` вперёд, расширяя окно, обновляя счётчики/условие.
2. Если условие ОКНА нарушено — двигай \`left\` вперёд, сужая окно, пока условие снова не выполнится.
3. На каждом шаге фиксируй лучший результат (например, максимальную длину окна).

## Пример: самая длинная подстрока без повторяющихся символов

Строка \`"abcabcbb"\`:

\`\`\`
right идёт по строке →
"abc[a]bcbb"   — символ 'a' повторился → двигаем left
    ↑left      пока повтор не исчезнет из окна
\`\`\`

\`\`\`go
func lengthOfLongestSubstring(s string) int {
	// ПРИНИМАЕТ: строку s. ВОЗВРАЩАЕТ: длину самой длинной подстроки без повторов.
	count := make(map[byte]int) // счётчик вхождений каждого символа в текущем окне
	left, best := 0, 0
	for right := 0; right < len(s); right++ {
		count[s[right]]++
		for count[s[right]] > 1 { // символ повторился в окне
			count[s[left]]-- // сужаем окно слева, убирая символы
			left++
		}
		windowLen := right - left + 1
		if windowLen > best {
			best = windowLen
		}
	}
	return best
}
fmt.Println(lengthOfLongestSubstring("abcabcbb")) // выведет: 3 ("abc")
\`\`\`

> Каждый символ строки заходит в окно и выходит из него РОВНО ОДИН РАЗ за весь проход — отсюда и линейная сложность O(n), несмотря на вложенный \`for\`.
`,
          homework: `
1. Прогони \`lengthOfLongestSubstring\` из урока вручную (на бумаге) для строки \`"pwwkew"\` — построй таблицу left/right/best по шагам, потом проверь код.
2. Модифицируй функцию, чтобы она возвращала САМУ подстроку, а не только её длину.
3. Реши вариацию: дан массив положительных чисел и число K — найди длину самого длинного непрерывного подмассива с суммой ≤ K, используя тот же принцип sliding window (счётчик суммы вместо map).
`,
          cards: [
            { q: "В чём суть техники sliding window?", a: "Два указателя left/right двигают «окно» по массиву/строке: right расширяет, left сужает при нарушении условия — вместо O(n²) перебора получаем O(n)" },
            { q: "Когда применять sliding window?", a: "Для задач на непрерывный подмассив/подстроку с условием (без повторов, сумма ≤ K и т.п.)" },
            { q: "Что делать при обнаружении повторного символа в окне?", a: "Двигать left вперёд, уменьшая счётчики, пока повтор не исчезнет из окна" },
            { q: "Почему sliding window даёт O(n), несмотря на вложенный цикл?", a: "Каждый элемент заходит в окно и выходит из него ровно один раз за весь проход — суммарно O(n) операций, а не O(n²)" }
          ]
        }
      ]
    },
    {
      id: "backend",
      title: "Модуль 6. Веб-бэкенд на Go",
      lessons: [
        {
          id: "go-17",
          title: "HTTP в Go: net/http, запросы и ответы",
          theory: `
## Модель клиент-сервер

\`\`\`
Client (браузер) → HTTP Request → Server (твой Go-код) → HTTP Response → Client
\`\`\`

## Методы HTTP

| Метод | Что делает |
|---|---|
| GET | Получить ресурс. Не должен менять состояние сервера |
| POST | Отправить данные для создания нового ресурса |
| PUT | Полностью заменить ресурс данными из запроса |
| PATCH | Частично изменить ресурс |
| DELETE | Удалить ресурс |

## Обработчик запроса — сигнатура, которую нужно знать наизусть

\`\`\`go
func handler(w http.ResponseWriter, r *http.Request) {
	// w (write) — сюда ПИШЕМ ответ. r (read) — отсюда ЧИТАЕМ запрос.
	// ПРИНИМАЕТ: оба параметра передаёт сам net/http при запросе. НИЧЕГО не возвращает.

	name := r.URL.Query().Get("name")
	// r.URL.Query().Get("key") ПРИНИМАЕТ имя GET-параметра.
	// ВОЗВРАЩАЕТ: строку-значение, или "" ПУСТУЮ СТРОКУ, если параметра нет.
	// ⚠️ Все параметры запроса ВСЕГДА строки — числа нужно парсить самому (strconv.Atoi).

	if name == "" {
		w.WriteHeader(400) // WriteHeader ПРИНИМАЕТ HTTP-статус, должен вызываться ДО записи тела
		fmt.Fprintln(w, "name обязателен")
		return
	}

	w.WriteHeader(200)
	fmt.Fprintln(w, "Hello,", name)
	// fmt.Fprintln(w, ...) — как обычный Println, но пишет в w вместо консоли
}
\`\`\`

> Если явно не вызвать \`w.WriteHeader\`, при первой записи в тело статус \`200\` отправится автоматически.

## Остальные методы Request и ResponseWriter

\`\`\`go
r.Method              // строка: "GET", "POST"...
r.URL.Path             // путь запроса, например "/hello"
r.Header.Get("User-Agent") // значение заголовка запроса
body, _ := io.ReadAll(r.Body) // прочитать тело запроса как []byte

w.Header().Set("Content-Type", "application/json") // задать заголовок ответа
\`\`\`

## Коды статуса — группы

| Диапазон | Смысл |
|---|---|
| 1xx | Информационные |
| 2xx | Успех (200 OK, 201 Created) |
| 3xx | Перенаправление (301 Moved Permanently) |
| 4xx | Ошибка КЛИЕНТА (400 Bad Request, 404 Not Found) |
| 5xx | Ошибка СЕРВЕРА (500 Internal Server Error) |
`,
          homework: `
1. Напиши обработчик для \`/hello\`, который читает GET-параметр \`name\`: если он пуст — возвращает статус 400 и текст ошибки, иначе — 200 и "Hello, <имя>".
2. Что вернёт \`r.URL.Query().Get("missing")\`, если параметра \`missing\` вообще нет в запросе? Проверь на практике и объясни, почему это может быть источником багов, если не проверять на пустую строку.
3. Совмести с уроком про JSON (go-07): напиши обработчик, который читает JSON из \`r.Body\` через \`io.ReadAll\` + \`json.Unmarshal\` в структуру и отвечает подтверждением.
`,
          cards: [
            { q: "Что принимает и возвращает функция-обработчик HTTP в Go?", a: "func(w http.ResponseWriter, r *http.Request) — w для записи ответа, r для чтения запроса; ничего не возвращает" },
            { q: "Что делает r.URL.Query().Get(\"name\")?", a: "Возвращает значение GET-параметра по ключу; если параметра нет — пустую строку \"\"" },
            { q: "Почему все параметры запроса приходят как string?", a: "URL — всегда текст; для чисел нужно парсить вручную через strconv.Atoi" },
            { q: "В каком порядке нужно вызывать WriteHeader и запись тела ответа?", a: "WriteHeader — ДО записи тела; если не вызвать явно, статус 200 отправится автоматически при первой записи" },
            { q: "Что означают группы HTTP статус-кодов 2xx/4xx/5xx?", a: "2xx — успех; 4xx — ошибка клиента (неверный запрос); 5xx — ошибка сервера" }
          ]
        }
      ]
    },
    {
      id: "utils",
      title: "Модуль 7. Полезности",
      lessons: [
        {
          id: "go-18",
          title: "Defer, битовые сдвиги и итераторы (Go 1.23)",
          theory: `
## defer — отложенное выполнение

\`\`\`go
func main() {
	defer fmt.Println("world") // ВЫПОЛНИТСЯ последним, после всего кода main
	fmt.Println("hello")
}
// Вывод:
// hello
// world
\`\`\`

Несколько \`defer\` накладываются друг на друга и выполняются в ОБРАТНОМ порядке (LIFO — «последний зашёл, первый вышел»):

\`\`\`go
for i := 0; i < 3; i++ {
	defer fmt.Println(i)
}
fmt.Println("done")
// Вывод:
// done
// 2
// 1
// 0
\`\`\`

## Битовые сдвиги — быстрое умножение/деление на степень двойки

\`\`\`go
x << n // ЭКВИВАЛЕНТНО x * (2^n)
1 << 1  // = 2  (двоичное 1 → 10)
1 << 10 // = 1024

x >> n // ЭКВИВАЛЕНТНО x / (2^n), целочисленно
8 >> 1  // = 4  (1000 → 100)
8 >> 2  // = 2  (1000 → 10)
\`\`\`

## Итераторы (Go 1.23+) — ленивая генерация значений

Обычная функция, возвращающая \`[]User\` из миллиона записей, сразу выделяет память под ВЕСЬ срез. Итератор генерирует значения ПО ОДНОМУ, по запросу цикла \`for\`:

\`\`\`go
func Squares(yield func(int) bool) {
	// СИГНАТУРА ИТЕРАТОРА: func(yield func(T) bool).
	// yield ПРИНИМАЕТ очередное значение T, ВОЗВРАЩАЕТ bool:
	//   true  — цикл хочет ЕЩЁ значения, продолжай
	//   false — цикл остановлен (break), пора завершить генерацию
	for i := 1; i <= 5; i++ {
		if !yield(i * i) {
			return // ОБЯЗАТЕЛЬНО проверяй возврат yield — иначе генерация продолжится впустую после break
		}
	}
}

func main() {
	for val := range Squares { // Squares передаётся БЕЗ вызова — сам range его вызывает как итератор
		if val > 10 {
			break
		}
		fmt.Println(val)
	}
}
// Вывод: 1 4 9
// (16 не генерируется — как только val=16 не проверился циклом, yield(16) вернёт false)
\`\`\`

Для пар значений (индекс + элемент) используется \`iter.Seq2\`:

\`\`\`go
import "iter"
func iterateAllUsers() iter.Seq2[int, User] { ... }
for idx, user := range iterateAllUsers() { ... }
\`\`\`

Готовые итераторы есть в стандартной библиотеке: \`slices.All(slice)\` (прямой порядок), \`slices.Backward(slice)\` (обратный).
`,
          homework: `
1. Предскажи на бумаге вывод: \`for i := 0; i < 5; i++ { defer fmt.Println(i*i) }\`, затем проверь кодом.
2. Посчитай в уме \`1 << 5\` и \`40 >> 3\`, потом проверь через \`fmt.Println\`.
3. Напиши свой итератор \`Evens(yield func(int) bool)\`, который генерирует чётные числа от 2 до 20, и используй его в \`for\` с ранним \`break\` при значении > 10.
`,
          cards: [
            { q: "В каком порядке выполняются несколько defer в одной функции?", a: "В обратном порядке (LIFO) — последний defer выполнится первым" },
            { q: "Что делает x << n?", a: "Быстрый эквивалент x * (2^n) — сдвиг битов влево" },
            { q: "Что делает x >> n?", a: "Быстрый эквивалент x / (2^n) — сдвиг битов вправо, целочисленно" },
            { q: "Какая сигнатура у функции-итератора в Go 1.23?", a: "func(yield func(T) bool) — генерирует значения лениво, по одному, по запросу цикла for" },
            { q: "Что означает false, возвращённый из yield?", a: "Цикл остановлен (например, break) — итератор должен прекратить генерацию (return)" }
          ]
        }
      ]
    }
  ]
});
